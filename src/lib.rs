//! Workspace root stub crate.
//!
//! This empty library exists so `cargo fmt`, `cargo clippy`, `cargo build`,
//! and `cargo test` have at least one target while `workspace.members` stays
//! `[]`. It is removed (or repointed) when the first member crate is added by
//! plan 01 (`crates/types`).
